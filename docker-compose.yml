services:
  app:
    build:
      context: .
      target: base
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
      - ./data:/app/data
      - ./outputs:/app/outputs
      - ./ui:/app/ui
    environment:
      - PYTHONPATH=/app/src
    command: ["python", "-m", "ultrasound.api"]

  test:
    profiles: ["test"]
    build:
      context: .
      target: test
    command: ["pytest", "tests/", "-v", "--tb=short"]
